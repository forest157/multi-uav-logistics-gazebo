# 决策 0001：水平比例与语义高度

状态：已采用。

水平比例采用 0.2 m/unit。它把 500 x 500 参考地图转换为适合 Iris SITL 的
100 x 100 m 场地，同时保持障碍物和通道的相对比例。若按 1 unit = 1 m，场景会
造成过长的 SITL 任务和不必要的计算开销；若比例过小，多机安全间距将无法保持。

Z 方向不使用 0.2 比例直接推导，因为参考数据没有高度。高度按对象语义赋值，范围
0.3–35 m。飞行高度必须由后续三维规划依据障碍高度和安全余量决定，不能固定为一个
能越过所有障碍的高度，否则会失去三维避障意义。
