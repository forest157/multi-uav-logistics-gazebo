# 多无人机物流仿真版本历史与后续路线图

本文档对应仓库 `forest157/multi-uav-logistics-gazebo` 的实际 Git 标签和当前开发方向。已发布版本记录已经完成并验证的能力；后续版本是开发计划，只有完成对应验收后才能合并到 `main` 并创建标签。

当前主线从双包结构统一为 `catkin_ws/src/logistics_gazebo_sim`：0.4.1 的 OMPL、TOPPRA、动态避障和上位机实现作为唯一持续开发版本；统一前的两个目录可通过既有版本标签恢复。

## 总体架构方向

```text
任务与场景配置
    ↓
OMPL 全局三维路径与编队可行性分析
    ↓
TOPPRA 速度/加速度时间参数化
    ↓
MPC 局部轨迹优化（后续版本）
    ↓
3D ORCA 多机速度安全约束
    ↓
安全监督器：减速 / 悬停 / 重规划 / 返航 / 降落
    ↓
MAVROS + PX4 + Gazebo，最终迁移到实机
```

基本原则：优先保持编队；安全约束高于队形保持；算法先影子模式、再受限接管、最后完整闭环；先仿真验证，再进入真实无人机测试。

# 已发布版本

## v0.1.0：可恢复三机基线

标签：`v0.1.0`，基线提交：`51740da`。

完成内容：

- 建立 Git/GitHub 可恢复工程；
- 保存稳定包和 ROS 三维实验包；
- 固化七种三维物流场景；
- 三架 PX4 SITL、MAVROS 和 Gazebo 基础任务；
- 起飞、编队飞行、投递、返航和降落基本链路；
- 建立设计、测试和交付文档目录。

## v0.2.0：三维规划、净空分析和自动队形调度

标签：`v0.2.0`，发布提交：`0974afa`。

完成内容：

- ROS OMPL Informed RRT* 三维全局规划；
- OMPL PathSimplifier 与 B 样条平滑；
- TOPPRA 三维速度和加速度参数化；
- 三维障碍净空、起终点容量和阶段可行性分析；
- 参数变化后的自动重规划和分类错误提示；
- 横队、纵队、垂直错层、三维楔形和螺旋等队形基础；
- 自动选择安全队形及变换窗口；
- rqt 上位机、地图点选、进度和分析展示。

## v0.2.1：降落标志碰撞修复

标签：`v0.2.1`，发布提交：`39546c6`。

完成内容：

- 移除起点/终点绿色标志的物理碰撞影响；
- 修复无人机落在标志碰撞体上导致的弹跳；
- 保留可视化标志但不参与 Gazebo 碰撞。

## v0.2.2：受控软降落

标签：`v0.2.2`，发布提交：`fa4dad1`。

完成内容：

- 限制最终下降速度；
- 使用平滑下降曲线保持已有更慢轨迹；
- 降低接地冲击，修复中间和两侧无人机落地弹跳；
- 完成三机软降落回归验证。

## v0.3.0：动态鸟类障碍与协同安全闭环

标签：`v0.3.0`，主线发布版本在提交 `6527358` 完成。

完成内容：

- Gazebo 一至两只平滑移动鸟类模型和真实碰撞体；
- 动态障碍状态、速度和短时轨迹预测；
- 三机未来轨迹发布和时间索引碰撞预测；
- 动态风险 SAFE、WARNING、CRITICAL 分级；
- WARNING 同步减速、CRITICAL 同步悬停；
- 整队三维偏移候选，保持机间相对位置；
- 动态障碍、静态场景、边界、高度和机间距联合校验；
- 可撤销绕行状态机和安全恢复；
- 规划器进程隔离、超时重试和故障安全回退；
- 完整投递、返航、软降落和实体包裹验收；
- 修复上位机外部仿真停止、启动提示和 PX4 环境问题。

关键验收：完整任务 `COMPLETE`，动态障碍无接触，三机保持物理安全距离并最终解除武装。

## v0.4.1：可插拔局部避障与 3D ORCA 影子模式

标签：`v0.4.1`，发布提交：`ce5fcac`。

完成内容：

- 建立统一局部避障规划器接口；
- 保留 `collective_offset` 稳定闭环算法；
- 新增 `orca3d` 逐机三维速度算法；
- 正面对撞确定性破对称；
- 最大速度、机间距、安全缓冲和动态净空复核；
- 算法不可行时保持减速/悬停安全回退；
- 风险监测、RViz、CSV、安全监控和包裹管理接口支持 1～8 架；
- 上位机显示当前避障算法和 ORCA 影子建议；
- 修复稳定包与实验包 README 中文乱码；
- 61 项回归测试通过，catkin 构建无警告。

Gazebo 影子模式验收：任务约 266.4 秒完成，捕获 68 个非零 ORCA 建议，实际最小机间距 2.876 m，安全监测器无 ERROR，三机最终解除武装。ORCA 尚未直接控制 PX4。

# 当前开发状态

- 当前稳定标签：`v0.4.1`；
- 当前主分支：统一单包结构，能力与 v0.4.1 一致；
- 下一开发分支：`feature/v0.4.2-orca-limited-control`；
- 0.4.2 必须依次通过离线回放、不开桨数据流、受限短航段、完整任务四级门槛，任何一级失败都保持 ORCA 影子模式。

# 后续版本规划

## v0.4.2：ORCA 受限闭环接管

目标：让 ORCA 第一次真实影响 PX4 飞行，同时保持独立安全监督。

实施阶段：

1. 冻结统一的逐机速度命令、时间戳、有效期和失败原因接口；
2. 增加限速、限加速度、限爬升率、变化率限制、滤波和滞回；
3. 建立独立命令安全门，仅允许通过动态净空、机间距、边界、高度和队形误差复核的命令下发；
4. 在不开桨模式验证话题链路、超时、无解和算法进程崩溃回退；
5. 先在高净空低速单鸟场景进行短航段接管，再进行完整投递返航；
6. 保留一键切回 `collective_offset` 和同步悬停的能力。

计划内容：

- 将逐机 ORCA 速度转换为 MAVROS 可执行的速度/位置目标；
- 增加速度、加速度、爬升率和指令变化率限制；
- 低通滤波和滞回，避免方向抖动；
- 将队形误差和机间距加入接管前复核；
- ORCA 超时、无解或候选变化时立即同步悬停；
- Gazebo/RViz 显示 ORCA 速度箭头、预测轨迹和实际轨迹；
- 首先测试无人机与鸟处于相同高度的单鸟横穿，再测试迎面和多鸟。

验收要求：

- 关闭旧整队偏移后，仍能观察到由 ORCA 产生的真实绕行动作；
- 无无人机碰撞、无鸟类接触；
- 最小机间距和障碍净空满足配置；
- 指令连续、跟踪误差可控；
- 无解时悬停回退可靠；
- 完成投递和返航。

## v0.4.3：分布式 MPC 影子模式

目标：增加考虑动力学、队形和能耗的局部轨迹优化器。

计划内容：

- 将 MPC 实现为与 ORCA 相同接口的可切换插件；
- 每架无人机运行局部 MPC，避免集中式 `3N` 维优化随数量快速膨胀；
- 约束位置、速度、水平/垂直加速度和爬升率；
- 代价函数包含全局轨迹跟踪、动态障碍净空、机间距、队形误差、控制平滑度和能耗；
- 超时或无解时回退到 ORCA，再失败则悬停；
- 先离线回放，再进入三机 Gazebo 影子模式。

验收要求：求解频率、最坏耗时和无解率满足实时要求；输出轨迹通过独立安全复核；不接管飞控时不影响现有任务。

## v0.4.4：MPC 受限闭环与算法对比

目标：比较 ORCA、MPC 和现有整队偏移算法的实际效果。

统一指标：

- 任务成功率；
- 最小动态障碍净空；
- 最小机间距；
- 最大跟踪误差；
- 速度和加速度连续性；
- 绕行距离与任务耗时；
- 计算平均值、95/99 分位和超时次数；
- 估算能耗。

通过数据决定 MPC 与 ORCA 的主次关系，不提前锁死单一算法。

## v0.5.0：真实传感器链路和动态目标跟踪

目标：逐步取消避障算法对 Gazebo 真值的依赖。

计划内容：

- 优先模拟三维激光雷达，资源不足时使用深度相机；
- 点云预处理、地面/静态背景过滤和动态聚类；
- 多目标数据关联、速度估计和短时轨迹预测；
- 遮挡、噪声、延迟和短时丢失处理；
- Gazebo 真值只用于误差评估和对照；
- 感知异常时降级到减速、悬停或真值实验模式。

## v0.5.1：电池与能量模型

目标：建立可重复、可标定的能量估计。

模型至少考虑悬停、水平速度、爬升/下降、加速度、转弯、载荷、电池容量差异和安全储备。后续与 MAVROS/PX4 电池状态进行标定。

输出包括任务剩余能耗、返航能耗、等待和降落能耗、安全储备及最终能量余量。

## v0.5.2：电量感知返航和槽位调整

目标：返航原则上保持编队，同时降低低电量无人机的任务代价。

计划内容：

- 低电量无人机分配更短、爬升更少、转弯更少的返航槽位；
- 在开阔区安全交换槽位；
- 根据电量安排降落顺序；
- 预计无法安全返航时选择备用降落点；
- 只有进入临界状态才允许脱队；
- 上位机显示剩余电量、返航需求和安全余量。

## v0.6.0：五至八架 PX4 与分组通行

目标：解除三机 PX4/MAVROS launch 的硬编码。

计划内容：

- 参数化 PX4 实例、MAVROS、端口、命名空间和出生位置；
- 启动前检查 CPU、内存、端口和场景容量；
- 按单机、三机、五机、八机顺序逐级验证；
- 整体编队无法通过时自动分组并分时穿越；
- 保持最小安全距离，不用危险压缩替代规划；
- 提供 20～50 架轻量运动学压力测试模式。

## v0.7.0：rqt 上位机重构

目标：让界面适应算法切换、车辆增加和复杂任务管理。

计划内容：

- 动态车辆状态表；
- ORCA/MPC/混合算法选择与参数配置；
- 动态障碍预测轨迹和冲突对象；
- 电量、返航能耗、槽位和降落顺序；
- 规划失败分类、恢复建议和安全事件；
- 任务保存、加载、重置和报告导出；
- 显示 Git 提交、算法版本、场景版本和配置摘要；
- 高频数据由后端聚合后显示，避免拖慢 rqt。

## v0.8.0：仿真到实机迁移准备

目标：保持同一任务和算法接口，从仿真逐步迁移到真实无人机。

计划内容：

- 消除 Gazebo 专用真值依赖；
- 参数、坐标系、时间同步和通信质量检查；
- 硬件在环和室内定位测试；
- 单机实测后再进入多机；
- 设置地理围栏、人工接管、失联和紧急降落机制；
- 实机测试严格限制高度、速度、区域和参与数量。

## v0.9.0：规模、故障与长期稳定性

目标：完成发布前压力测试。

覆盖长时间运行、规划器崩溃、通信延迟、传感器丢失、电池异常、单机失效、多障碍和任务重复执行，建立自动化报告与回归门槛。

## v1.0.0：稳定发布

只有在以下条件全部满足后发布：

- 核心任务具有可重复的自动验收；
- ORCA/MPC/全局重规划和安全回退边界明确；
- 电量返航与异常处置经过验证；
- 配置规模在声明范围内稳定；
- 仿真和实机能力边界分别说明；
- README、安装、恢复、测试和版本文档完整。

# Git 与发布规则

- `main` 只保存已验证、可恢复版本；
- 新功能使用 `feature/*`，修复使用 `fix/*`；
- 每个版本先在功能分支完成测试，再快进合并到 `main`；
- 标签使用 `v主版本.次版本.修订版本`；
- 版本标签发布后不移动、不覆盖；
- 测试 CSV、指标摘要和失败复盘写入 `reference`，大型运行日志不提交；
- 任何“算法有效”结论必须区分影子模式和真实闭环。
